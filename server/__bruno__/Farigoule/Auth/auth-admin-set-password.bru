meta {
  name: auth/admin-set-password
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/auth/admin-set-password
  body: json
  auth: inherit
}

body:json {
  {
      "userId" : 12,
      "newPassword" : "test"
  }
}

settings {
  encodeUrl: true
}
